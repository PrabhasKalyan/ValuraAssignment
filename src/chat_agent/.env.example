DB_PATH=chat.db
MAX_TURNS_PER_CONV=20
MAX_ACTIVE_CONVS=1000
MEMORY_TOP_K=5

# Mem0 long-term memory. Default: ON.
# Mem0 needs an LLM + embedder. Defaults are OpenAI gpt-4o-mini + text-embedding-3-small,
# so set OPENAI_API_KEY for the default config to work.
MEM0_ENABLED=true
OPENAI_API_KEY=